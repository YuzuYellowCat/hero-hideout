import React from "react";
import "./index.css";
import LoadingBox from "components/LoadingBox";

const PageLoader: React.FC = () => {
    return <LoadingBox className="page-loader" />;
};

export default PageLoader;
